# Global Gateway Frontend - Environment Variables Example
# Copy this file to .env and fill in your actual values

# NOTE: This project no longer includes a dedicated backend by default.
# The Request Catalog modal uses the user's client to send messages:
#  - Send Email: opens the system mail client (mailto:)
#  - Send WhatsApp: opens https://wa.me/ with a prefilled message

# Company email address (used as recipient for catalog requests when user chooses Email)
REACT_APP_COMPANY_EMAIL=contact@theglobalGateway.in

# Optional: if you deploy a serverless function (Netlify Functions, Vercel Serverless, etc.)
# to send emails from the server, set its endpoint here (e.g. '/.netlify/functions/send-email')
# Leave empty to use client-side mailto/WhatsApp flow.
# REACT_APP_EMAIL_API_ENDPOINT=/.netlify/functions/send-email

# WebSocket Configuration (optional)
# For development server with HTTPS
# WDS_SOCKET_PORT=443

# Feature Flags (development)
# NODE_ENV can be 'development', 'production', or 'test'
# NODE_ENV=development
